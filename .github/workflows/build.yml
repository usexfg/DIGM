# NOTE: This workflow is superseded by ci.yml.
# Kept only to prevent breaking existing branch protection rules.
# Remove once CI requires ci.yml checks instead.
name: Build (legacy)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is superseded by ci.yml. All checks run there."
