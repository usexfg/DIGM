name: Build Android App

on:
  push:
    branches: [ main, develop, originsapp ]
    paths:
      - 'device-proof-recorder-android/**'
  pull_request:
    branches: [ main, develop, originsapp ]
    paths:
      - 'device-proof-recorder-android/**'

jobs:
  build-android:
    name: Build Android App
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
      
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
      
      - name: Install Fastlane
        run: |
          cd device-proof-recorder-android
          bundle install
      
      - name: Grant execute permission for gradlew
        run: |
          cd device-proof-recorder-android
          chmod +x gradlew || true
      
      - name: Build Android App
        env:
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
          CI: 'true'
        run: |
          cd device-proof-recorder-android
          bundle exec fastlane ci
      
      - name: Upload Debug APK
        uses: actions/upload-artifact@v3
        with:
          name: android-debug-apk
          path: |
            device-proof-recorder-android/app/build/outputs/apk/debug/*.apk
          retention-days: 7
          if-no-files-found: ignore
      
      - name: Upload Release AAB
        uses: actions/upload-artifact@v3
        with:
          name: android-release-aab
          path: |
            device-proof-recorder-android/app/build/outputs/bundle/release/*.aab
          retention-days: 30
          if-no-files-found: ignore

  test-android:
    name: Test Android App
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
      
      - name: Grant execute permission for gradlew
        run: |
          cd device-proof-recorder-android
          chmod +x gradlew || true
      
      - name: Run tests
        run: |
          cd device-proof-recorder-android
          ./gradlew test --no-daemon

