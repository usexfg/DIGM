name: digm_core
description: DIGM music platform bridge — business logic layer on top of Fuego blockchain.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
